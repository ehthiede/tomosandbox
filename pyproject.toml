[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
authors = [
   { name = "MY NAME", email = "my.email@address.com"},
]
name = "tomo_sandbox"
version = "0.0.1"
dependencies = [
    "numpy",
    "finufft >= 2"
]
